include ../partials/header.pug
include ../partials/top_nav.pug

form#sign_up_form(action='/login' method='post')
  .form-group
    input#user_name.form-control(type='text' name='user_name' placeholder='UserName' required='required')
    input#psw.form-control(type='password' name='password' placeholder='Password' required='required')
  .form-group
    button#my_submit_button.btn.btn-primary.btn-block.login-btn(type='submit' onclick='this.form.submit()') Login
if(d)
  strong #{d}
